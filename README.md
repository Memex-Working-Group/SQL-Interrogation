# SQL Interrogation

I want to understand the data models and queries behind the use cases of software.

In this repo we provide the instructions to,

* Run software
* Extract the software's schema
* Document the software's schema
* Document how the use cases become queries

# Use Virtual ENV

``` bash
python3 -m venv env
source env/bin/activate
pip install eralchemy2
pip install psycopg2-binary
```